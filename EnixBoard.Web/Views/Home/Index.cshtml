@using EnixBoard.Web.Controllers;
@model EnixBoard.Web.Models.JoinModel
@{
    ViewBag.Title = "Home";
}
@*<div class="row form-horizontal">
        <div class="col-sm-8 col-sm-offset-2">
            New game ? @Html.ActionLink("Go!", "NewGame", null, new { @class = "btn btn-primary" })
        </div>
    </div>
    <hr />*@
<div class="row form-inline">
    @using (Html.BeginForm("JoinGame", "Home", FormMethod.Get))
    {
        <div class="col-sm-8 col-sm-offset-2 form-group">
            <div class="">
                @Html.LabelFor(m => m.GameType, "Select your game type", new { @class = "col-sm-3" })
                @Html.DropDownListFor(m => m.GameType, HomeController.GameList, new { @class = "form-control col-sm-6" })
                <div class="col-sm-4">
                    <button type="submit" class="btn btn-primary btn-block">
                        <i class="glyphicon glyphicon-star"></i> Go
                    </button>
                </div>
            </div>
        </div>
    }
</div>
@section scripts{

}